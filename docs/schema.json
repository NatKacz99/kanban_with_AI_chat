{
  "tables": [
    {
      "name": "users",
      "fields": [
        { "name": "id", "type": "integer", "constraints": ["primary_key"] },
        { "name": "username", "type": "text", "constraints": ["unique", "required"] },
        {"name": "password_hash", "type": "text", "constraints": ["required"] },
        { "name": "created_at", "type": "datetime" }
      ]
    },
    {
      "name": "boards",
      "fields": [
        { "name": "id", "type": "integer", "constraints": ["primary_key"] },
        { "name": "user_id", "type": "integer", "constraints": ["foreign_key: users.id"] },
        { "name": "name", "type": "text" },
        { "name": "created_at", "type": "datetime" }
      ]
    }
  ],
  "relationships": [
    { "from": "boards.user_id", "to": "users.id", "type": "many-to-one" }
  ]
}